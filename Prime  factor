#include<stdio.h>
int main()
{
    int num,i,j[100];
    scanf("%d", &num);
    for (i = 2;i <= num;i++)
    {
        while (num % i == 0)
        {
            printf("%d\n", i);
            num /= i;
        }
    }
}
