#include<stdio.h>
int main()
{
    int l,r,sum=0,i;
    scanf("%d%d",&l,&r);
    for(i=l;i<=r;i++)
    {int t=i;
        do
        {
          int num;
          num=t%10;
          if(num==2)  sum++;
        }while(t/=10);
    }
    printf("%d",sum);
    return 0;
}
