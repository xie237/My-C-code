#include<iostream>
#include<algorithm>
using namespace std;
int main()
{
	int n,num[110];
	cin >> n;
		for (int i = 1;i <= n;i++)
		{
			cin >> num[i];
	     }
	reverse(num + 1, num + 1 + n);
	for (int i = 1;i <= n;i++)
	{
		cout << num[i] << " ";
	}
}
